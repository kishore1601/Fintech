import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kishorefintech.app',
  appName: 'Fintech App',
  webDir: 'dist/fin/browser'
};

export default config;
